display:
  default:
    display_options:
      empty:
        add_content_by_bundle:
          label: 'Propose a session'
        area_text_custom:
          content: 'There are no sessions submitted that meet this criteria.'
      title: 'Submitted Sessions'
      exposed_form:
        options:
          submit_button: Filter
          reset_button_label: Reset
          exposed_sorts_label: 'Sort by'
      filters:
        field_audience_target_id:
          expose:
            label: Audience
        field_session_category_target_id:
          expose:
            label: Category
        combine:
          expose:
            label: Search
      header:
        add_content_by_bundle:
          label: 'Propose a session'
        result:
          content: '<div class="results-summary"><strong>Results:</strong> @start to @end of @total proposals</div>'
      footer:
        result:
          content: '<div class="results-summary"><strong>Results:</strong> @start to @end of @total proposals</div>'
    display_title: Default
  page_1:
    display_title: Page
    display_options:
      menu:
        title: Sessions
